## 1. Info Tab Line Wrapping

- [x] 1.1 Create helper function to manually wrap text with indentation in `render_info_tab`
- [x] 1.2 Update task rendering to use manual wrapping instead of `Wrap` widget
- [x] 1.3 Remove `Wrap` from Info tab `Paragraph` widget

## 2. Agent Tab Blank Line Compression

- [x] 2.1 Add consecutive blank line filtering in `render_message_lines` function
- [x] 2.2 Add consecutive blank line filtering in `render_done_section` function
